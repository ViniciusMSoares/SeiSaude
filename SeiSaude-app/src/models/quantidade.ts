export class Quantidade {
    nome: string;
    unidade: string;
    valor: number;

    public constructor(nome, unidade, valor) {
        this.nome = nome;
        this.unidade = unidade;
        this.valor = valor;
    }
}
